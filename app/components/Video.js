/** @jsx React.DOM */
var React = require('react');

var Video = React.createClass({
    render: function() {
        return (
            <video></video>
        );
    }
    
});
    
module.exports = Video;
